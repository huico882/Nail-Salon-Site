import { Box, Typography } from "@mui/material";
import React from "react";

export const Sidebar = () => {
  return (
    <Box flex={2} p={2} bgcolor="coral">
      <Typography>SideBar</Typography>
    </Box>
  );
};
